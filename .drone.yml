kind: pipeline
type: docker
name: greeting

steps:
- name: en
  image: alpine
  environment:
    HELLO:
      from_secret: test_secret
  commands:
  - echo $HELLO

- name: fr
  image: alpine
  commands:
  - echo bonjour monde

trigger:
  branch:
    exclude:
    - main
    - master